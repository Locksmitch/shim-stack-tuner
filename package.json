{
  "name": "shim-stack-tuner",
  "version": "1.0.0",
  "private": true,
  "description": "Valving Toolbox - browser-based suspension valving calculators (shim stack tuner, spring curve calculator, shim delta tool, oil viscosity comparison)",
  "scripts": {
    "start": "node server.js",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test test/*.test.mjs",
    "test:e2e": "node test/smoke.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.7.0",
    "playwright": "^1.62.0",
    "prettier": "^3.9.6"
  }
}
